import { App } from "@/app/documentation/react-polygrid/start/_snippets/polygrid";
import raw from "@/app/documentation/react-polygrid/start/_snippets/polygrid?raw";

# Get Started

Quick guide on how to use {<code>PolyGrid</code>} in a React app.

## Installation

Install the package in your project.

```sh npm2yarn
npm install @jsoc/react-polygrid
```

## Usage

- Import the component

```ts
import { PolyGrid } from "@jsoc/react-polygrid";
```

- Render the component

- Code:
  {/* Add a helper component to toggle between code and output */}

  <pre>{raw}</pre>

- Output:
  <App />
