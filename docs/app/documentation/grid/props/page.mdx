import { generateDefinition, TSDoc } from "nextra/tsdoc";

# Props

<TSDoc
  definition={generateDefinition({
    code: `
import type { JsocGridProps } from "@jsoc/react/grid";
type Props = JsocGridProps<"mui" | "ag">
export default Props`,
  })}
/>
