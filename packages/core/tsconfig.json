{
	"compilerOptions": {
		// target
		"target": "ES2022", // to support newer methods like Array.at
		"moduleResolution": "bundler",
		"lib": ["ES2022", "DOM"],

		// strict checks
		"strict": true,
		"strictNullChecks": true,
		"strictFunctionTypes": true,
		"noUnusedLocals": true,

		// output
		"sourceMap": true,
		"skipLibCheck": true,

		// source files
		// "baseUrl": "./src",
		// "paths": {
			/*
			 * Monorepo issue: Consumer packages fail to resolve types from this package that depend on
			 * internal path aliases (e.g., '@/utils'). 
			 * Example:
			 * Define a type in any file of @jsoc/core
			 * ```type ABC = Array<XYZ>```  // type XYZ in defined in other file of @jsoc/core and imported using type alias, lets say '@/utils'
			 * Now if we import type ABC `@jsoc/react`, its type will shows as `any`.
			 * Other the other hand if we import using relative import like "../../utils" it would show correct type.

			 * This is because the consumer's TypeScript server doesn't inherit this package's `paths` configuration.
			 * TypeScript project references were considered but did not resolve this issue.
			 */
			// "@/*": ["*"]
		// }
	},

	"exclude": [
		"dist",
		"build",
		"**/*.min.js",
		"node_modules",
		"**/node_modules/*"
	]
}
